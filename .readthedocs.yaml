version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"
    rust: "latest"
  jobs:
    pre_install:
      - pip install -U pip
      - pip install .[docs]

sphinx:
  builder: html
  configuration: docs/conf.py
  fail_on_warning: false

